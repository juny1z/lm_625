% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_lm.R
\name{fast_lm}
\alias{fast_lm}
\title{Fast Linear Regression (pure R implementation)}
\usage{
fast_lm(formula, data, x, y, intercept = TRUE)
}
\arguments{
\item{formula}{A model formula (e.g. \code{y ~ x1 + x2}). Optional if
\code{x} and \code{y} are supplied.}

\item{data}{Optional data frame containing the variables in the formula.}

\item{x}{Optional numeric matrix (or object coercible to matrix) of predictors.}

\item{y}{Optional numeric response vector.}

\item{intercept}{Logical; for the \code{x}/\code{y} interface only, whether to
include an intercept column (default \code{TRUE}).}
}
\value{
An object of class \code{"fastlm"} with components:
\itemize{
\item \code{coefficients} Named numeric vector of regression coefficients.
\item \code{fitted.values} Numeric vector of fitted values.
\item \code{residuals} Numeric vector of residuals.
\item \code{sigma2} Residual variance estimate.
\item \code{call} The matched call.
\item \code{formula} The model formula (if used).
\item \code{terms} The terms object (if formula interface used).
\item \code{model.matrix} The model matrix used for fitting.
\item \code{response} The response vector.
}
}
\description{
\code{fast_lm()} fits an ordinary least squares (OLS) linear model
using explicit matrix formulas. The interface is similar to \code{lm()},
and the estimated coefficients are identical to those from \code{lm()}
(up to numerical precision).

You can use either a formula interface (\code{y ~ x1 + x2}) with a
data frame, or an \code{x}/\code{y} interface with numeric matrices/vectors.
}
\examples{
set.seed(1)
n  <- 100
x1 <- rnorm(n)
x2 <- rnorm(n)
y  <- 1 + 2 * x1 - 3 * x2 + rnorm(n)
dat <- data.frame(y, x1, x2)

fit_fast <- fast_lm(y ~ x1 + x2, data = dat)
fit_lm   <- lm(y ~ x1 + x2, data = dat)

all.equal(coef(fit_fast), coef(fit_lm))

}
